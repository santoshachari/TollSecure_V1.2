<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
	xmlns:context="http://www.springframework.org/schema/context"
    xmlns:tx="http://www.springframework.org/schema/tx"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xmlns:aop="http://www.springframework.org/schema/aop"
	xsi:schemaLocation="
		http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context
		http://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/mvc
		http://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/tx 
		http://www.springframework.org/schema/tx/spring-tx.xsd
		http://www.springframework.org/schema/aop
		http://www.springframework.org/schema/aop/spring-aop.xsd">

	<!-- Adding  AspectJ autoproxy support for AOP-->
	<aop:aspectj-autoproxy/>
	
	<!-- Add support for component scanning -->
	<!-- <context:component-scan base-package="com.seoniproject" /> -->
	<context:component-scan base-package="com.tollsecure" />

	<!-- Add support for conversion, formatting and validation support -->
	<mvc:annotation-driven/>

	<!-- Define Spring MVC view resolver -->
	<bean
		class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="prefix" value="/WEB-INF/view/" />
		<property name="suffix" value=".jsp" />
	</bean>

    <!-- Step 1: Define Database DataSource / connection pool -->
	<bean id="myDataSource" class="com.mchange.v2.c3p0.ComboPooledDataSource"
          destroy-method="close">
        <property name="driverClass" value="com.mysql.jdbc.Driver" />
        <property name="jdbcUrl" value="jdbc:mysql://localhost:3306/seoni_db?useSSL=false" />
        <property name="user" value="seoni_user" />
        <property name="password" value="seoni_password" /> 

        <!-- these are connection pool properties for C3P0 -->
        <property name="minPoolSize" value="5" />
        <property name="maxPoolSize" value="20" />
        <property name="maxIdleTime" value="30000" />
	</bean>  
	
    <!-- Step 2: Setup Hibernate session factory -->
	<bean id="sessionFactory"
		class="org.springframework.orm.hibernate5.LocalSessionFactoryBean">
		<property name="dataSource" ref="myDataSource" />
		<property name="packagesToScan" value="com.tollsecure.entity" />
		<property name="hibernateProperties">
		   <props>
		      <prop key="hibernate.dialect">org.hibernate.dialect.MySQLDialect</prop>
		      <prop key="hibernate.show_sql">true</prop>
		   </props>
		</property>
   </bean>	  

    <!-- Step 3: Setup Hibernate transaction manager -->
	<bean id="myTransactionManager"
            class="org.springframework.orm.hibernate5.HibernateTransactionManager">
        <property name="sessionFactory" ref="sessionFactory"/>
    </bean>
    
    <!-- Step 4: Enable configuration of transactional behavior based on annotations -->
	<tx:annotation-driven transaction-manager="myTransactionManager" />

	<!-- adding static resources (css, images, js, etc) -->
	<mvc:resources location="/resources/" mapping="/resources/**"></mvc:resources>

	<!-- for ubuntu -->
	<mvc:resources mapping="/Wallpapers/**" location="file:///home/anjan/Pictures/Wallpapers/"> </mvc:resources>

	<!-- for windows -->
	<!-- <mvc:resources mapping="/vehicleImages/**" location="file:///C:/Users/Admin/Pictures/vehicleImages/"> </mvc:resources> -->	
	<!-- <mvc:resources mapping="/vehicleImages/**" location="file:///C:/Program Files/TollSecure/vehicleImages/"> </mvc:resources> -->
	<mvc:resources mapping="/vehicleImages/**" location="file:///C:/TollSecure/vehicleImages/"> </mvc:resources>
	
	<!-- for Ubuntu reports -->
	<mvc:resources mapping="/Desktop/**" location="file:///home/anjan/Desktop/"> </mvc:resources>
	
	<!-- for windows reports -->
	<!-- <mvc:resources mapping="/JasperReports/**" location="file:///C:/Program Files/TollSecure/JasperReports/"> </mvc:resources> -->
	<mvc:resources mapping="/JasperReports/**" location="file:///C:/TollSecure/JasperReports/"> </mvc:resources>
	<mvc:resources mapping="/barCodes/**" location="file:///C:/TollSecure/barCodes/"> </mvc:resources>
</beans>





